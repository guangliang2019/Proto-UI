id: C-A11Y-0001
type: contract
title: Accessibility exposes projectable semantic objects
status: draft
since: 0.1.0
summary: The a11y domain records projectable semantic object facts such as identity, role, name, description, state, action, relation, and semantic tree behavior.
statement:
  zh-CN: >-
    Accessibility 是一个可由宿主投影的 semantic object domain。原型通过 a11y API 声明当前交互对象的 identity、role、name、description、state、action、relation 与 semantic tree behavior；runtime 将这些声明保存为稳定 IR，adapter 负责把 IR 投影到宿主的无障碍表达。a11y IR 可以引用 state/focus/event/anatomy 等其它域事实，但 a11y 不重新拥有这些域。


  en: >-
    Accessibility is a host-projectable semantic object domain. A prototype declares identity, role, name, description, state, action, relation, and semantic tree behavior for the current interaction object through a11y APIs; the runtime stores these declarations as stable IR, and adapters project the IR to host accessibility output. A11y IR may reference facts from state, focus, event, anatomy, and other domains, but a11y does not re-own those domains.


criteria:
  - id: C-A11Y-0001-A
    text:
      zh-CN: a11y domain 必须输出可由 adapter 投影的 semantic object IR。
      en: The a11y domain must output semantic object IR that adapters can project.
  - id: C-A11Y-0001-B
    text:
      zh-CN: semantic object 至少应能表达 identity、role、name、description、state、action 与 relation。
      en: A semantic object should at least be able to express identity, role, name, description, state, action, and relation.
  - id: C-A11Y-0001-C
    text:
      zh-CN: role 表达交互对象是什么，不得直接等同于某个宿主 attribute 名称。
      en: Role describes what the interaction object is and must not be equated directly with a host attribute name.
  - id: C-A11Y-0001-D
    text:
      zh-CN: name 与 description 必须作为 semantic object 的事实记录，而不是 Button 等单个原型的私有 props 规则。
      en: Name and description must be recorded as semantic object facts rather than private props rules of a single prototype such as Button.
  - id: C-A11Y-0001-E
    text:
      zh-CN: a11y state 可以引用 state handle；其值读取与生命周期仍由 state domain 负责。
      en: A11y state may reference state handles; value reading and lifecycle remain owned by the state domain.
  - id: C-A11Y-0001-F
    text:
      zh-CN: a11y action 可以引用 expose event 或其它交互通路；action 本身不重新定义 event route。
      en: A11y action may reference expose events or other interaction routes; the action itself does not redefine the event route.
  - id: C-A11Y-0001-G
    text:
      zh-CN: adapter 可以按宿主能力选择原生 identity/role/name/state/action/relation 投影、降级投影或仅保留诊断 IR。
      en: Adapters may choose native identity/role/name/state/action/relation projection, degraded projection, or diagnostic-only IR based on host capabilities.
  - id: C-A11Y-0001-H
    text:
      zh-CN: a11y relation 可以引用字符串目标或 state handle；其语义表示对象关系，宿主 adapter 负责投影为宿主可表达的关系机制。
      en: A11y relation may reference a string target or state handle; it represents an object relationship, and host adapters are responsible for projecting it to host-expressible relationship mechanisms.
  - id: C-A11Y-0001-I
    text:
      zh-CN: a11y name 与 description 可以引用字符串或 state handle；当引用 state handle 时，adapter 投射必须随 state 值变化更新。
      en: A11y name and description may reference a string or a state handle; when they reference a state handle, adapter projection must update as the state value changes.
  - id: C-A11Y-0001-J
    text:
      zh-CN: a11y role 可以引用静态字符串或 state handle；当交互对象的 role 由受治理状态切换时，adapter 投射必须随 state 更新。
      en: A11y role may reference a static string or a state handle; when an interaction object's role changes through governed state, adapter projection must update with that state.
  - id: C-A11Y-0001-K
    text:
      zh-CN: 对于可包含多个 IDREF 的 relation，原型可以声明追加投影；adapter 必须保留宿主已有且不由该 semantic object 拥有的 relation 目标，并只更新或移除自身拥有的目标。
      en: For relations that accept multiple IDREFs, a prototype may declare additive projection; the adapter must preserve existing host relation targets not owned by that semantic object and update or remove only its own targets.
  - id: C-A11Y-0001-L
    text:
      zh-CN: a11y semantic tree behavior 可以引用 state handle；投影 snapshot 必须解析为 boolean，并在 mounted 后随 state 更新，且 semantic hiding 不得误改宿主布局可见性。
      en: A11y semantic tree behavior may reference state handles; projected snapshots must resolve to booleans and update with state after mount, while semantic hiding must not alter host layout visibility.
  - id: C-A11Y-0001-M
    text:
      zh-CN: Web adapter 投影字符串 a11y state 时，空字符串必须移除对应宿主 attribute，并在恢复非空值后重新投影。
      en: When the Web adapter projects a string a11y state, an empty string must remove the corresponding host attribute, and a later non-empty value must restore it.
  - id: C-A11Y-0001-LEVEL
    text:
      zh-CN: >-
        a11y 语义对象可以携带 portable heading level。只有 resolved role 为 heading 且 level 是 1-6 的整数时，snapshot 才投影 level，Web adapter 将其投影为 aria-level。静态、初始 state-backed 与 final-setup 声明中的 0、负数、分数、NaN 和 Infinity 必须 fail closed 并被拒绝。setup 完成后的无效 state-backed runtime 值仍由 State 拥有；a11y 不得修改、否决或回滚该源，只从 snapshot 省略 level 并让 Web adapter 移除旧 aria-level，后续有效值必须恢复投影。非 heading role、target replacement、detach 或 disposal 必须清理旧物理 target 的 aria-level；runtime 值无效时不得改写独立治理的 role。


      en: >-
        The a11y semantic object may carry a portable heading level. A snapshot projects level only when the resolved role is heading and level is an integer from 1 through 6; the Web adapter projects it as aria-level. Static, initial state-backed, and final-setup declarations containing 0, negatives, fractions, NaN, or Infinity must fail closed and be rejected. After setup, an invalid state-backed runtime value remains owned by State; A11y must not mutate, veto, or roll back that source, must omit level from the snapshot so the Web adapter removes stale aria-level, and must restore projection after a later valid value. A non-heading role, target replacement, detach, or disposal must clear aria-level from the previous physical target; an invalid runtime level must not rewrite the independently governed role.


sources:
  - path: internal/records/2026-06-30-a11y-module-planning-for-button.zh-CN.md
  - path: packages/modules/a11y/src/types.ts
  - path: packages/modules/a11y/src/create.ts
  - path: packages/modules/a11y/src/web.ts
  - path: packages/runtime/test/contract/a11y.v0.contract.test.ts
  - path: packages/adapters/web-component/test/contract/a11y.v0.contract.test.ts
  - path: https://www.w3.org/TR/accname-1.2/
    label: W3C Accessible Name and Description Computation
  - path: https://www.w3.org/TR/wai-aria-1.2/
    label: WAI-ARIA 1.2
dependsOn:
  contracts:
    - C-CORE-SYNTAX-0003
    - C-STATE-0008
    - C-EXPOSE-EVENT-0001
relates:
  decisions:
    - D-A11Y-SEMANTIC-DOMAIN-0001
  prototypes:
    - P-BASE-BUTTON
verifies:
  tests:
    - T-A11Y-0001
revisions:
  - version: 0.1.0
    change: introduced
    summary: Introduced the minimal semantic object contract for the a11y module.
  - version: 0.1.1
    change: refined
    summary: Allowed a11y name and description text facts to reference state handles for dynamic host-neutral labels.
  - version: 0.1.2
    change: refined
    summary: Allowed semantic roles to reference state handles for governed dynamic role projection.
  - version: 0.2.0-rc.7
    change: refined
    summary: Added additive relation projection, state-backed semantic tree behavior, and optional Web a11y state omission.
  - version: 0.3.0-alpha.0
    change: refined
    summary: >-
      Added role-gated portable heading-level semantics with strict setup and final-setup validation, State-owned runtime invalid-value omission and recovery, and stale Web aria-level cleanup across role, target, and view-lifecycle transitions.


tags:
  - a11y
  - accessibility
  - semantic-object
  - host-projection
