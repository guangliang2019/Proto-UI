---
title: 'Expose'
desp: 'Expose 规范'
description: 'Expose 规范'
---

## 摘要

写作提示：

- 用一小段可读文字说明 `expose` 在 Proto UI 中承担什么角色。
- 说明它为何不是简单的“ref API”，而是组件向外提供可引用能力的一条正式通路。

## 这篇规范定义什么？

写作提示：

- 定义 `expose` 可暴露的对象类型、暴露时机与对外可见性语义。

## 对原型作者意味着什么？

写作提示：

- 说明原型作者什么时候应该使用 `expose`，什么时候不该用它替代其他通路。

## 对适配器作者意味着什么？

写作提示：

- 说明适配器可以选择宿主最佳实践来承载暴露能力，但不能改变其语义承诺。

## 正式语义

写作提示：

- 定义暴露对象、方法、状态引用等在协议层的意义。

## 有效行为 / 无效行为 / 未定义行为

写作提示：

- 裁决暴露同步性、可变性、宿主桥接方式等边界问题。

## 与其他规范的关系

写作提示：

- 说明它与 `props`、`state`、`event`、执行时期之间的区别。
