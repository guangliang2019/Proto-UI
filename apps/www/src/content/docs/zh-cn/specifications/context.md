---
title: 'Context'
desp: 'Context 规范'
description: 'Context 规范'
---

## 摘要

写作提示：

- 用一小段可读文字说明 `context` 作为组件与组件之间共享环境信息的正式通路。

## 这篇规范定义什么？

写作提示：

- 定义 `context` 的订阅、分发、传递方向与作用范围。

## 对原型作者意味着什么？

写作提示：

- 说明原型作者如何理解 `context` 与局部状态、显式输入之间的边界。

## 对适配器作者意味着什么？

写作提示：

- 说明适配器如何在宿主中承载 `context`，以及为什么它通常是较易保持一致的能力。

## 正式语义

写作提示：

- 定义上下文值、作用域、传播时机与更新规则。

## 有效行为 / 无效行为 / 未定义行为

写作提示：

- 重点裁决嵌套作用域、覆盖关系、动态更新时机等边界。

## 与其他规范的关系

写作提示：

- 说明它与 `props`、`state`、`expose` 的区别与联系。
