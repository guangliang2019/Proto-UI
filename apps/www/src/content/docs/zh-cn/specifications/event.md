---
title: 'Event'
desp: 'Event 规范'
description: 'Event 规范'
---

## 摘要

写作提示：

- 用一小段可读文字说明 `event` 代表什么，以及它为何是 User 指向组件的关键通路。

## 这篇规范定义什么？

写作提示：

- 定义 `event` 的语义角色、触发条件、回调关系与副作用边界。

## 对原型作者意味着什么？

写作提示：

- 说明原型作者如何声明自己关心的交互事件，以及事件在原型语义中的地位。

## 对适配器作者意味着什么？

写作提示：

- 说明适配器作者需要尽可能精准地翻译交互事件。
- 说明宿主事件模型可以不同，但语义相位不能乱。

## 正式语义

写作提示：

- 定义事件的输入方向、语义对应关系、触发时机与可见副作用边界。

## 有效行为 / 无效行为 / 未定义行为

写作提示：

- 重点处理“宿主事件判定略有差异是否可接受”这类边界。

## 与其他规范的关系

写作提示：

- 说明它与 `feedback`、`lifecycle`、`expose`、`state` 的关系。
