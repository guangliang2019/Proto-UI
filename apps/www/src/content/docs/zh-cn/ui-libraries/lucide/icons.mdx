---
title: '图标列表'
description: 'Lucide 图标列表，支持逐图标复制原型代码。'
---

import LucideIconGallery from '@/components/LucideIconGallery.astro';

当前页面收录 Proto UI 已启用的 Lucide 图标。每个图标卡片支持：

- 复制 `asHook` 使用片段。
- 复制内联 `render` 片段。
- 图标卡片进入视口后才按需加载对应模块。

<LucideIconGallery locale="zh-cn" />
