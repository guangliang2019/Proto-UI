---
title: Button
description: 按钮组件
---

import AdapterPanelScript from '@/components/PrototypePreviewer/AdapterPanelScript.astro';
import PrototypePreviewer from '@/components/PrototypePreviewer/PrototypePreviewer.astro';
import { codeMap } from '@/content/docs/demo_components/button/buttonCode';
import { getCode } from '@/utils/conversionUtils';

<div data-adapter-panel="wc">
  这是 wc 的 button 组件
  <PrototypePreviewer
    demoId="demo-button"
    initialRuntime="wc"
    toolbar={false}
    runtimes={['wc']}
    hasCode={true}
    code={getCode('wc', 'demo-button', codeMap)}
  />
  这是 wc 的 测试 组件
  <PrototypePreviewer
    demoId="demo-button"
    initialRuntime="wc"
    toolbar={false}
    runtimes={['wc']}
    hasCode={true}
    code={getCode('wc', 'demo-button', codeMap)}
  />
</div>

<div data-adapter-panel="react" style="display:none;">
  这是 react 的 button 组件
  <PrototypePreviewer
    demoId="demo-button"
    initialRuntime="react"
    toolbar={false}
    runtimes={['react']}
    hasCode={true}
    code={getCode('react', 'demo-button', codeMap)}
  />
</div>

<div data-adapter-panel="vue" style="display:none;">
  这是 vue 的 button 组件
  <PrototypePreviewer
    demoId="demo-button"
    initialRuntime="vue"
    toolbar={false}
    runtimes={['vue']}
    hasCode={true}
    code={getCode('vue', 'demo-button', codeMap)}
  />

    这是 vue 的 测试 组件

  <PrototypePreviewer
    demoId="demo-button"
    initialRuntime="vue"
    toolbar={false}
    runtimes={['vue']}
    hasCode={true}
    code={getCode('vue', 'demo-button', codeMap)}
  />
</div>

<AdapterPanelScript />
