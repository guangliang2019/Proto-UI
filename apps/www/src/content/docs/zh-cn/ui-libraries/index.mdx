---
title: 'UI Libraries'
description: '浏览当前基于 Proto UI 重构的 UI 库。'
---

Proto UI 对外展示的不是一套单一组件库，而是一组建立在 Proto UI 原型体系之上的 UI 库。

- `Base` 更接近协议层本身，强调可复用的交互语义边界。
- `Shadcn` 是参考型 UI 库，目标是在对应宿主中尽量贴近上游 shadcn/ui 的 API 与交互表现。

先选择库，再进入具体条目查看。
