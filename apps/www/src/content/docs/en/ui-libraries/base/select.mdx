---
title: 'Select'
desp: 'Select base prototype'
description: 'Select base prototype'
---

import { PrototypePreviewer } from '@/components/PrototypePreviewer';

`base-select` is the current base-library probe for value-class components.

<div>
  <PrototypePreviewer
    demoId="demo-base-select"
    initialRuntime="wc"
    runtimes={['wc', 'react', 'vue']}
    hasCode={true}
  />
</div>

Unlike `dropdown`, this family does not stop at open/close plus active-item coordination. It also includes a dedicated `select-value` part, which keeps selected text presentation inside the protocol boundary.

## Family Parts

- `select-root`: owns `open`, `value`, and `textValue`
- `select-trigger`: opens and closes the popup surface
- `select-value`: renders the selected text or placeholder
- `select-content`: owns overlay entry, dismissal, and focus entry
- `select-item`: contributes `value` / `textValue` snapshots and commits selection

## Why It Matters

This family is not only an addition to the base library. It is also a probe for Proto UI's current value-component path:

- selected value text is defined by the prototype family itself
- the host app does not have to render that text manually
- value presentation can be refreshed through explicit update-driven re-render

That makes `select-value` a concrete `render-consumed-value` sample instead of a purely exposed-state sample.

## Current v0 Boundary

`base-select` currently includes:

- single selection
- popup open/close coordination
- selected item focus entry
- selected text synchronization
- a protocol-owned value presentation part

It does not yet include:

- form semantics
- native `<select>` parity
- combobox/search behavior
- grouped options
- multi-select

## References

- Contract: [select.v0.md](/Users/yangguangliang/Desktop/projects/Proto-UI/internal/contracts/prototype-base/select.v0.md)
- Source: [packages/prototypes/base/src/select](/Users/yangguangliang/Desktop/projects/Proto-UI/packages/prototypes/base/src/select)
- Test: [select.test.ts](/Users/yangguangliang/Desktop/projects/Proto-UI/packages/prototypes/base/test/select.test.ts)
