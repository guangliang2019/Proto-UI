# Agent Operations readable projections

This directory explains operational boundaries that sit beside Proto UI's repository Agent tooling. It does not define Proto UI product semantics and it does not supersede `spec/**`, `internal/agent-operations/**`, repository rules, or live GitHub permission.

The subject is not currently cataloged by a `spec/**` entity. Documents here are transitional readable projections of explicitly reviewed operational gaps. Stable machine-enforced rules belong in `internal/agent-operations/**`; dated decisions and rollout evidence belong in `internal/records/**`.

- [Poppy review broker](./poppy-review-broker.md) projects the accepted #557 controller/applier boundary and its staged graduation state.
